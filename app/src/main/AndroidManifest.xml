<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name ="com.luanabarbosa.verity.githubapi.ui.GithubApp"
        android:allowBackup="true"
        android:icon="@mipmap/icon_github"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_github_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubAPI"
        tools:targetApi="31">
        <activity
            android:name=".ui.main.GithubMainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>